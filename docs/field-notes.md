# Field Notes

This note keeps the simulations assumptions visible beside the checks.

The domain cases cover `input pressure`, `state drift`, `review cost`, and `decision risk`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

`recovery` is the strongest case at 235 on `decision risk`. `stress` is the cautious anchor at 145 on `state drift`.

The language-specific addition keeps the review model in a small record-based API.
